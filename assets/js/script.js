const menuToggle=document.querySelector('.menu-toggle');const navLinks=document.querySelector('.nav-links');if(menuToggle&&navLinks){menuToggle.addEventListener('click',()=>navLinks.classList.toggle('open'))}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());document.querySelectorAll('form[data-demo-form]').forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault();const notice=form.querySelector('.notice');if(notice){notice.style.display='block';notice.textContent='Thanks! This demo form is ready to connect to your email, CRM, or Google Form endpoint.'}form.reset();})});document.querySelectorAll('.faq-item').forEach(btn=>{btn.addEventListener('click',()=>{const panel=btn.nextElementSibling;if(panel){panel.classList.toggle('open');btn.querySelector('strong').textContent=panel.classList.contains('open')?'–':'+'}})});

const spinBtn=document.querySelector('#spinBtn');const wheel=document.querySelector('#discountWheel');const result=document.querySelector('#discountResult');let rotation=0;const discounts=['5% Off','7% Off','10% Off','12% Off','15% Off','20% Off','$25 Off','$50 Off'];function softBeep(){try{const ctx=new (window.AudioContext||window.webkitAudioContext)();const osc=ctx.createOscillator();const gain=ctx.createGain();osc.connect(gain);gain.connect(ctx.destination);osc.frequency.value=660;gain.gain.setValueAtTime(.045,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.14);osc.start();osc.stop(ctx.currentTime+.15)}catch(e){}}if(spinBtn&&wheel){spinBtn.addEventListener('click',()=>{const index=Math.floor(Math.random()*discounts.length);const segment=360/discounts.length;const current=rotation%360;const targetAngle=360-(index*segment+segment/2);const extraSpins=360*5;rotation+=extraSpins+targetAngle-current;wheel.style.transform=`rotate(${rotation}deg)`;spinBtn.disabled=true;result.textContent='Spinning...';setTimeout(()=>{result.textContent=`You landed on: ${discounts[index]}`;softBeep();spinBtn.disabled=false},4100)})}

const castLeadBtn=document.querySelector('#castLeadBtn');const restartLeadBtn=document.querySelector('#restartLeadBtn');const restartLeadBtnModal=document.querySelector('#restartLeadBtnModal');const viewCaughtBtn=document.querySelector('#viewCaughtBtn');const caughtList=document.querySelector('#caughtList');const catchCount=document.querySelector('#catchCount');const leadGameStatus=document.querySelector('#leadGameStatus');const fishingStage=document.querySelector('.fishing-stage');const fishTargets=document.querySelectorAll('.fish');const leadModal=document.querySelector('#leadModal');const closeLeadModal=document.querySelector('#closeLeadModal');const leadSummary=document.querySelector('#leadSummary');const mockLeads=[{business:'Maple Street Bakery',need:'Needs a cleaner menu page and online order buttons',area:'Downtown family shop',urgency:'Warm lead'},{business:'Iron Oak Landscaping',need:'Website is missing service areas, gallery photos, and quote form',area:'Suburban service route',urgency:'High potential'},{business:'Cornerstone Barber Co.',need:'Needs booking links, reviews, and stronger mobile layout',area:'Main street storefront',urgency:'Ready soon'},{business:'Northside Auto Repair',need:'No clear emergency contact path or service breakdown',area:'Local garage district',urgency:'Strong fit'},{business:'Little Lantern Daycare',need:'Needs trust-focused copy, parent FAQs, and tour request form',area:'Family neighborhood',urgency:'Nurture lead'},{business:'Riverbend Yoga Studio',need:'Class schedule is hard to update and hard to read on phones',area:'Wellness district',urgency:'Warm lead'},{business:'Summit Roofing Crew',need:'Needs storm-damage landing page and quote funnel',area:'Regional contractor',urgency:'High potential'},{business:'Cedar Pet Grooming',need:'Needs before-and-after gallery and appointment request form',area:'Local plaza',urgency:'Ready soon'}];let caughtLeads=[];let lineIsCast=false;function renderCaughtLeads(){if(!caughtList||!catchCount)return;catchCount.textContent=caughtLeads.length;caughtList.innerHTML='';if(caughtLeads.length===0){caughtList.innerHTML='<li class="empty-catch">No leads caught yet. Cast the line, then click a moving fish.</li>';return}caughtLeads.forEach((lead,index)=>{const li=document.createElement('li');li.innerHTML=`<strong>${index+1}. ${lead.business}</strong><span>${lead.need}</span><span>${lead.area} • ${lead.urgency}</span>`;caughtList.appendChild(li)})}function setFishPaused(paused){fishTargets.forEach(fish=>fish.classList.toggle('paused',paused))}function resetLeadGame(){caughtLeads=[];lineIsCast=false;renderCaughtLeads();setFishPaused(false);if(fishingStage)fishingStage.classList.remove('casting','ready-to-catch');if(castLeadBtn){castLeadBtn.disabled=false;castLeadBtn.textContent='Cast the line'}if(viewCaughtBtn)viewCaughtBtn.disabled=true;if(leadGameStatus)leadGameStatus.innerHTML='<strong>Ready to cast?</strong> Catch 5 mock leads to finish the round. Cast the line, then click a moving fish before it swims away.';if(leadModal){leadModal.classList.remove('open');leadModal.setAttribute('aria-hidden','true')}}function openLeadModal(){if(!leadModal||!leadSummary)return;leadSummary.innerHTML=`<div class="lead-summary-grid">${caughtLeads.map(lead=>`<div class="lead-summary-card"><h3>${lead.business}</h3><p><strong>Need:</strong> ${lead.need}</p><p><strong>Area:</strong> ${lead.area}</p><p><strong>Status:</strong> ${lead.urgency}</p></div>`).join('')}</div>`;leadModal.classList.add('open');leadModal.setAttribute('aria-hidden','false')}function catchFish(fish){if(!lineIsCast||caughtLeads.length>=5)return;const remaining=mockLeads.filter(lead=>!caughtLeads.some(caught=>caught.business===lead.business));const lead=remaining[Math.floor(Math.random()*remaining.length)];caughtLeads.push(lead);lineIsCast=false;renderCaughtLeads();softBeep();fish.classList.add('caught-pop');setTimeout(()=>fish.classList.remove('caught-pop'),450);if(fishingStage)fishingStage.classList.remove('casting','ready-to-catch');if(caughtLeads.length>=5){setFishPaused(true);if(castLeadBtn){castLeadBtn.disabled=true;castLeadBtn.textContent='Round complete'}if(viewCaughtBtn)viewCaughtBtn.disabled=false;if(leadGameStatus)leadGameStatus.innerHTML='<strong>Round complete!</strong> You caught 5 mock leads. View the catches or restart the game.'}else{if(castLeadBtn){castLeadBtn.disabled=false;castLeadBtn.textContent='Cast again'}if(leadGameStatus)leadGameStatus.innerHTML=`<strong>Caught ${lead.business}!</strong> ${5-caughtLeads.length} more lead${5-caughtLeads.length===1?'':'s'} to go. Cast again, then click another moving fish.`}}if(castLeadBtn){renderCaughtLeads();fishTargets.forEach((fish,index)=>{fish.setAttribute('role','button');fish.setAttribute('tabindex','0');fish.setAttribute('aria-label',`Catch fish ${index+1}`);fish.addEventListener('click',()=>catchFish(fish));fish.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();catchFish(fish)}})});castLeadBtn.addEventListener('click',()=>{if(caughtLeads.length>=5)return;lineIsCast=true;castLeadBtn.disabled=true;if(fishingStage)fishingStage.classList.add('casting','ready-to-catch');if(leadGameStatus)leadGameStatus.innerHTML='<strong>Line cast!</strong> Click one of the moving fish to catch a mock lead.';setTimeout(()=>{if(lineIsCast){lineIsCast=false;if(fishingStage)fishingStage.classList.remove('casting','ready-to-catch');castLeadBtn.disabled=false;if(leadGameStatus)leadGameStatus.innerHTML='<strong>Missed the catch.</strong> Cast again and click a fish while the line is in the water.'}},4200)})}if(viewCaughtBtn)viewCaughtBtn.addEventListener('click',openLeadModal);if(restartLeadBtn)restartLeadBtn.addEventListener('click',resetLeadGame);if(restartLeadBtnModal)restartLeadBtnModal.addEventListener('click',resetLeadGame);if(closeLeadModal)closeLeadModal.addEventListener('click',()=>{leadModal.classList.remove('open');leadModal.setAttribute('aria-hidden','true')});
